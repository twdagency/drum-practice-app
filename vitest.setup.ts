import '@testing-library/jest-dom';

// Note: Database setup/teardown is handled in individual test files
// or can be added here if needed. The setup is optional and tests
// will work with mocks if database is not available.
